# Rename this file to "credentials.py" and fill in your credentials before running the server
# Ignore this message, if you are running a docker container and read the docs (Docker Hub repo) instead

funk_username = "name@domain.com"
funk_password = "SomePassword"